// Isto é um código não executável!

// Procedural
processamento(valor1, valor2, valor3)

// Orientado a Objetos
objeto = {
  valor1,
  valor2,
  valor3,
  processamento() {
    //...
  }
}
objeto.processamento() // Foco passou a ser o objeto

// Princípios importantes

// 1. Abstração;
// 2. Encapsulamento (OBS: Acoplamento grande não é uma boa prática);
// 3. Herança (OBS: Priorizar a composição à herança);
// 4. Polimorfismo
