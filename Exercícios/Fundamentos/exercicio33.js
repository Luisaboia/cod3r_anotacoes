let vetorInteiro = [1, 2, 3, 4]
let vetorString = ['Um', 'Dois', 'Três', 'Quatro']
let vetorDouble = [1.1, 2.2, 3.3, 4.4]

const intStr = vetorInteiro.concat(vetorString)
const resultado = vetorDouble.concat(intStr)
console.log(resultado)
